# HAMCD runtime config (minimal)

video:
  width: 1280
  height: 720
  fps: 60

hand_detection:
  static_image_mode: false
  max_num_hands: 2
  min_detection_confidence: 0.7
  min_tracking_confidence: 0.5

palm_writing:
  enabled: true
  dominant_hand: "right"
  role_assignment_mode: "position"
