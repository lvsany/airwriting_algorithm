# 算法流程说明

## 系统概述

系统名称为 HAMCD（Hover-Anchored Mahalanobis Contact Detection），目标是在无接触传感器条件下，利用双目摄像头和手部关键点检测，实时判断书写手指尖是否与非主导手掌面发生接触，并记录书写轨迹。

---

## 模块组成

系统由五个模块组成，依次调用：

1. `DualHandDetector` — 主检测器，负责调度各子模块
2. `PalmLocalFrame` — 掌面坐标系构建与维护
3. `HandFeatureExtractor` — 逐帧提取 10 维特征向量
4. `HoverAnchorDetector` — Hover 校准与在线接触检测
5. `ContactStateMachine` — 接触状态防抖与 onset 精确化

---

## 逐帧处理流程

### 1. 图像输入与手部检测

摄像头以 1280×720、60fps 采集帧。每帧转换为 RGB 后送入 MediaPipe Hands，最多检测两只手，输出每只手的 21 个三维关键点（landmark）。

检测结果按图像横坐标排序，图像左侧为左手，图像右侧为右手。当 MediaPipe 标签与位置不一致时，以位置为准进行修正。

### 2. 角色分配

根据配置中的主导手字段（默认右手），将两只手分别标记为书写手（writing）和画板手（palm）。书写手为右手，画板手为左手。

### 3. 掌面坐标系构建（PalmLocalFrame）

每帧用画板手的关键点 0、5、9、17 构建正交坐标系：

- 原点 O = landmark 0（手腕）
- u 轴 = normalize(lm5 − lm17)，即食指 MCP 指向小指 MCP
- 法向量 n = normalize(u × v_raw)，其中 v_raw = normalize(lm9 − O)
- v 轴 = normalize(n × u)，与 u 正交

当向量共线或长度退化时，保留上一帧坐标系。

### 4. 特征提取（HandFeatureExtractor）

每帧从书写手和画板手提取 10 维特征向量，维度定义如下：

| 维度 | 名称 | 计算方式 |
|------|------|----------|
| 0–4 | dist2d_lm0/5/9/13/17 | 书写手指尖到画板手各关键点的 2D 像素欧氏距离 |
| 5 | hull_iou | 双手凸包交集与并集面积之比 |
| 6 | local_n | 指尖在掌面坐标系下的 n 分量（法向深度） |
| 7 | roi_brightness | 指尖 32×32 ROI 灰度均值 |
| 8 | roi_shadow | 指尖 32×32 ROI 拉普拉斯方差 |
| 9 | approach_theta | 指尖运动方向与掌面法向量的夹角（度，EMA 平滑） |

无法计算的维度填 NaN。

### 5. Hover 校准与接触检测（HoverAnchorDetector）

检测器分三个阶段运行：

**阶段一：等待稳定（waiting）**

检测 dim 0（dist2d_lm0）的滑动标准差。当最近 10 帧的标准差低于 5.0 像素时，进入采集阶段。

**阶段二：采集基线（collecting）**

连续采集 150 帧（约 3 秒）的 hover 特征向量，建立统计基线：

- μ = 各维度 nanmedian
- σ = 1.4826 × MAD（绝对中位差），用于估计鲁棒标准差
- τ = hover 帧距离分布的第 99 百分位数，作为自适应阈值

距离 D 定义为对角协方差近似的马氏距离，仅使用维度 {0, 7, 8, 9}：

$$D = \left\| \frac{f_{\{0,7,8,9\}} - \mu_{\{0,7,8,9\}}}{\sigma_{\{0,7,8,9\}}} \right\|_2$$

选取这四个维度的依据是：它们在 contact 时相对 hover 基线一致减小，z 归一化后各分量同时为负且幅度大，因此 D 在接触时单调增大并超过阈值 τ。其余维度的排除原因如下：

| 维度 | 排除原因 |
|------|----------|
| 1–4（指尖到各手指 MCP 距离） | 接触位置不固定，变化方向和幅度因落点而异，信噪比低 |
| 5（双手凸包 IoU） | 接触时增大，趋势与其他维度相反，混入计算会稀释信号 |
| 6（掌面坐标系 n 分量） | 来源于 MediaPipe z 轴深度估计，精度有限，噪声过大，仅保留用于辅助方向判断 |

四个选入维度在 contact 时的变化趋势：

| 维度 | 含义 | hover → contact 趋势 | 物理解释 |
|------|------|----------------------|----------|
| 0 | 指尖到手腕 2D 距离 | 减小 | 指尖向掌面靠近，到手腕距离缩短；同时用作方向门控（z[0] < 0） |
| 7 | 指尖 ROI 灰度均值 | 减小 | 指尖与掌面接触产生遮挡和阴影，局部亮度下降 |
| 8 | 指尖 ROI 拉普拉斯方差 | 减小 | 接触后指尖区域纹理被压平，边缘锐度降低 |
| 9 | 指尖运动方向与掌面法向量夹角 | 减小（趋近 0°） | 接触时运动方向垂直靠近掌面，角度比自由悬停时更小 |

**阶段三：在线检测（ready）**

每帧计算归一化特征向量 z = (f − μ) / σ，再计算 D。

接触判定规则：

- 进入接触：D > τ × ENTER_SCALE（=1.0），且 z[0] < 0（指尖更靠近掌面）
- 保持接触：D > τ × EXIT_SCALE（=1.05）
- 退出接触时不检查方向约束，防止状态卡死

### 6. 接触状态机（ContactStateMachine）

对 raw_contact 进行防抖处理。状态切换规则：

- IDLE → CONTACT：连续 4 帧 raw_contact = True
- CONTACT → IDLE：连续 4 帧 raw_contact = False

任意一帧方向与目标相反，待确认计数归零。

切换为 CONTACT 时，回溯最近 8 帧的距离序列，取 argmax(D[t] − D[t−1]) 对应帧号作为精确 onset，用于减少约 50ms 的检测延迟。

### 7. 静止保持检测（Still Hold）

当接触状态为 CONTACT 且书写手指尖的像素位移连续 36 帧（约 0.6 秒）不超过 20 像素时，触发 still_hold 事件。

触发后，系统强制将接触状态设为 IDLE，保存当前笔画，清空画布，并设置 30 帧冷却期以防止误触发。

### 8. 轨迹记录与输出

接触状态为 CONTACT 且不在冷却期时，每帧记录一个轨迹点，包含：

- (x, y)：屏幕像素坐标（书写手 landmark 8 的投影）
- (u, v)：掌面坐标系下的平面坐标

笔画结束后，轨迹以 CSV 格式保存。

---

## 数据输出

每次会话产生三类文件：

- `session_*.csv`：逐帧日志，包含特征向量、距离、状态等
- `session_*_stroke_N.csv`：每条笔画的轨迹点序列
- `session_*_ui.mp4` / `session_*_raw.mp4`：带可视化叠加的视频和原始帧视频
